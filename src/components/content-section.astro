---
export interface Props {
  id: string;
  title: string;
}

const { title, id } = Astro.props;
---

<section
  id={id}
  class="flex flex-col gap-4 space-y-8 ml-10"
>
  <div class="flex flex-col gap-4">
    <slot name="eyebrow" />
    <h2
      class="font-extrabold tracking-tight text-5xl"
    >
      {title}
    </h2>
  </div>
  <div class="flex flex-row gap-20">
    <slot name="image" />
    <slot name="paragraph"/>
  </div>
  <slot name="foot" />
</section>
